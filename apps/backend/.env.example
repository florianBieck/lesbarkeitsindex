# PostgreSQL connection string
DATABASE_URL=postgresql://admin:admin@localhost:5432/lesbarkeitsindex?schema=public

# R NLP sidecar (Docker only, ~60s startup, health: /health)
R_SIDECAR_URL=http://localhost:8787
